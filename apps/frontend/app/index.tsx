import { SafeAreaView, Text } from 'react-native';

const Page = () => {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <Text>Hello, world!</Text>
    </SafeAreaView>
  );
};

export default Page;
